{
  "name": "e2e-tests",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0"
  },
  "scripts": {
    "test": "playwright test",
    "posttest": "node scripts/summarize.js",
    "install-browsers": "playwright install --with-deps",
    "summarize": "node scripts/summarize.js"
  }
}
